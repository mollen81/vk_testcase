version: '3.8'
services:
  tarantool:
    image: tarantool/tarantool:2.11
    ports:
      - "3301:3301"
    volumes:
      - ./config.yml:config.yml
      - ./init.lua:init.lua
    environment:
      TARANTOOL_USER_NAME: guest